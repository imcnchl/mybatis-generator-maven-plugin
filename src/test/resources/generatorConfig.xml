<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE generatorConfiguration PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd" >
<generatorConfiguration>
    <!--
    修改一下两个属性
    <sqlMapGenerator targetPackage
    javaClientGenerator targetPackage
    -->
    <context id="permission">

        <!-- 注释 -->
        <commentGenerator>
            <property name="javaFileEncoding" value="UTF-8"/>
            <!-- 是否取消注释 -->
            <property name="suppressAllComments" value="false"/>
            <!-- 是否生成注释代时间戳-->
            <property name="suppressDate" value="true"/>
        </commentGenerator>

        <!-- jdbc连接 -->
        <jdbcConnection driverClass="com.mysql.jdbc.Driver"
                        connectionURL="jdbc:mysql://127.0.0.1:3306/mybatis_generator_example?useInformationSchema=true"
                        userId="root"
                        password="root"/>

        <!-- 类型转换 -->
        <javaTypeResolver>
            <!-- 是否使用bigDecimal， false可自动转化以下类型（Long, Integer, Short, etc.） -->
            <property name="forceBigDecimals" value="false"/>
            <property name="useJSR310Types" value="true"/>
        </javaTypeResolver>

        <!-- 生成实体类地址 -->
        <javaModelGenerator targetPackage="cn.caohongliang.mybatis.generator.example"
                            targetProject="src/test/java">
            <property name="enableSubPackages" value="true"/>
            <!-- 是否针对string类型的字段在set的时候进行trim调用 -->
            <property name="trimStrings" value="false"/>
        </javaModelGenerator>

        <!-- 生成mapxml文件 -->
        <sqlMapGenerator targetPackage="mapper"
                         targetProject="src/test/resources">
            <property name="enableSubPackages" value="true"/>
        </sqlMapGenerator>

        <!-- 生成mapxml对应client，也就是接口dao -->
        <javaClientGenerator targetPackage="cn.caohongliang.mybatis.generator.example"
                             targetProject="src/test/java" type="XMLMAPPER">
            <property name="enableSubPackages" value="true"/>
        </javaClientGenerator>

        <!-- 配置表信息 -->
        <table tableName="user"
               domainObjectName="user.entity.User"
               mapperName="user.dao.UserDao">
            <property name="subPackage" value="aa"/>
            <generatedKey column="id" sqlStatement="MySQL" identity="true"/>
        </table>

        <table tableName="product"
               domainObjectName="product.entity.Product"
               mapperName="product.dao.ProductDao">
            <generatedKey column="id" sqlStatement="MySQL" identity="true"/>
        </table>
        <table tableName="article"
               domainObjectName="product.entity.Article"
               mapperName="product.dao.ArticleDao">
            <generatedKey column="id" sqlStatement="MySQL" identity="true"/>
        </table>

    </context>
</generatorConfiguration>